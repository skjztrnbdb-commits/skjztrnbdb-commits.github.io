# nexthardstep — studio site

Static site of the NEXT (HARD) STEP game studio, served by GitHub Pages from
`main`. Studio home at the root, one folder per game (`shape-evade/` holds the
App Store support and privacy pages). Source of truth for every public web
page of the studio; nothing here is generated from anywhere else.
